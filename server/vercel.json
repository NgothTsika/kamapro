{
  "buildCommand": "npm run generate && npm run build",
  "outputDirectory": "dist",
  "functions": {
    "dist/api/index.js": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/api/index.js"
    }
  ]
}
